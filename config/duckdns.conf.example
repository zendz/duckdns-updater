# DuckDNS Configuration
# Copy this file to /etc/duckdns.conf and set proper permissions:
#   sudo cp config/duckdns.conf.example /etc/duckdns.conf
#   sudo chmod 600 /etc/duckdns.conf

# Your DuckDNS domain (without .duckdns.org)
DUCKDNS_DOMAIN=mydomain

# Your DuckDNS token from https://www.duckdns.org/
DUCKDNS_TOKEN=your-token-here

# Check interval in seconds (default: 300 = 5 minutes)
CHECK_INTERVAL=300

# Log file location
LOG_FILE=/var/log/duckdns.log

# Enable IPv6 support (default: true)
# Set to false if your EC2 instance doesn't have IPv6 or you don't want to update IPv6
ENABLE_IPV6=true
